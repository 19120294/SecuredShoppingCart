chinh lai ip cua datasource theo ip cua may co mysql
chinh lai data init khi chay lan dau
//        createDefaultUserIfNotExits();
        createDefaultRoleIfNotExits(defaultRoles);
//        createDefaultAdminIfNotExits();


Project intern KLB
Build a secured shopping cart backend with java spring boot and MySQL ( java 17/ Maven/ MySQL )
- Model: User, role, product, order, item, cart, category
- Service: User service, product service, order service, image service, cart service, category service
- Technologies: I18N, Logging, AccessLog ( Log4j2 ), Validation, Rest Client, Handler, MockServer, Unit Testing,
Rest API Response, Functional Programming, API Documentation, Cluster Database, Spring Security ( JWT )
